# 个性化陪读阅读室｜Codex 项目规则

本文件约束在本目录中工作的 Codex。除非用户明确改变规则，每次项目工作都必须遵守。

## 1. 核心原则

1. 学习效果优先于功能数量、角色包装和技术展示。
2. 严格轨与陪读轨分离；陪读表达不得添加严格轨中不存在的事实。
3. 论文事实、公式、图表和实验结论必须能够定位到原文。
4. 验证集、测试集和竞赛测试结果必须明确区分。
5. 不把模型自动生成的答案当成其自身正确性的证明。

## 2. 每项任务的固定工作流

```text
明确本次学习目标
→ 获取并保存权威原始材料
→ 检查文本、公式、图片和页码
→ 建立证据地图
→ 制作不含答案的学习者版
→ 制作独立的评分与诊断规则
→ 用正确、部分正确、错误答案进行内部演练
→ 记录验证结果
→ 更新项目进展与下一步
→ 用 Markdown 向用户报告
```

## 3. 强制状态更新

每次完成实际工作后，在结束回复前必须更新 `docs/progress/current.md`：

- 在“最近完成”中记录本次完成内容；
- 写明对应文件和验证证据；
- 更新阶段状态；
- 在“接下来要做”中保持一个明确的最高优先级任务；
- 已完成事项从待办区移入完成区；
- 不得仅在聊天中报告而不更新文档。

如果任务尚未完成，应记录真实状态和阻塞原因，不得标记完成。

## 4. 最终回复格式

最终回复使用 Markdown，至少包括：

```markdown
## 已完成

## 生成或修改的文件

## 验证结果

## 接下来自动进行
```

简单问答不要求机械套用格式；只要本轮修改了项目文件，就必须使用上述结构。

## 5. SENet 首轮实验约束

- 原始材料以 CVPR 2018 官方开放论文为准。
- 首轮只验证第 3 节 SE block，不要求用户读完整篇论文。
- 第一轮核心学习目标：
  1. 理解显式通道重标定；
  2. 正确推导 Squeeze、Excitation、Scale 的张量形状；
  3. 理解 SE 在 ResNet residual 分支中的插入位置。
- 学习者作答前不得看到 `02-评测与诊断规则.md`。
